COMANDOS INICIAIS:
python -m venv venv
./venv/Scripts/Activate 
python -m pip install --upgrade pip

PIP:
opencv-python
boxmot
ultralytics
DRIVERS PARA UTILIZAÇÃO DE CUDA NO TREINAMENTO DO MODELO:
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118  
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

CLONES:
https://github.com/damo-cv/TransReID.git


ALTERAÇÕES NECESSÁRIAS:
C:\Users\Yanzera\Documents\DEV\TRANSREID_TESTE\venv\Lib\site-packages\boxmot\appearance\reid\auto_backend.py
import torch.nn as nn
super().__init__() 

        # --- CÓDIGO A SER ADICIONADO ---
        # Verifica se um modelo pré-inicializado foi passado em vez de um caminho de arquivo
        if isinstance(weights, nn.Module):
            self.model = weights
            self.model_name = getattr(weights, 'name', weights.__class__.__name__)
            self.backend = 'PyTorch'
            self.device = device
            self.half = half
            # Pula o resto da inicialização original que lida com arquivos
            return


C:\Users\Yanzera\Documents\DEV\SenseVision\TransReID\model\backbones\vit_pytorch.py
TROCAR from torch._six import container_abcs 
POR import collections.abc as container_abcs